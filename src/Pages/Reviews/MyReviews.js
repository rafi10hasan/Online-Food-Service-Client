import React from 'react';
import useTitle from '../../Hooks/useTitle';

const MyReviews = () => {
    useTitle('myreviews');
    return (
        <div>
            
        </div>
    );
};

export default MyReviews;